import React, { Component } from 'react';

export default class CreateRoomPage extends Component {
    constructor(props) {
        // call parent constructor (Component constructor)
        super(props);
    }

    render() {
        return <p>This is the create join page</p>;
    }
}